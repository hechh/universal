syntax = "proto3";
package pb;
option go_package = "../../common/pb";

// 错误码配置表
enum ErrorCode {
	 Success = 0;
}

// 枚举配置表1
enum ROUTE {
	 UID = 0;
	 RegionID = 1;
}

// 枚举配置表2
enum SEND {
	 Point = 0;
	 Broadcast = 1;
}

// 枚举配置表2
enum SERVER {
	 Gate = 0;
	 Game = 1;
	 Db = 2;
	 Gm = 3;
}
syntax = "proto3";
package universal;

option go_package = "universal/common/pb";

// 错误码
enum ErrorCode {
    SUCCESS          = 0;  // 成功
    FAILED           = 1;  // 失败
    TIMEOUT          = 2;  // 超时
    CONFIG_NOT_FOUND = 3;  // 配置文件不存在
    REQUEST_PARAM    = 4;  // 请求参数错误
}
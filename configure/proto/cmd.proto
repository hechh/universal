
/*
 * 本代码由cfgtool工具生成，请勿手动修改
 */

syntax = "proto3";

package universal;

option go_package = "universal/common/pb";

enum CMD {
    CMD_EMPTY      = 0;         // 空参数
    LOGIN_REQUEST  = 16777216;  // 登录接口
    LOGIN_RESPONSE = 16777217;  // 登录接口
    HEART_REQUEST  = 16777218;  // 心跳接口
    HEART_RESPONSE = 16777219;  // 心跳接口
}


/*
* 本代码由cfgtool工具生成，请勿手动修改
*/

syntax = "proto3";

package universal;

option go_package = "universal/common/pb";

import "global_enum_cfg.proto";

message RouterConfig {
  uint32   Cmd       = 1;  // 消息唯一ID（一定是偶数）
  string   Request   = 2;  // 请求协议名
  string   Response  = 3;  // 应答协议名
  NodeType NodeType  = 4;  // 节点类型
  string   ActorName = 5;  // Actor名称
  string   FuncName  = 6;  // 请求方法名称
}

message RouterConfigAry {
  repeated RouterConfig Ary = 1;
}
